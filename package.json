{
  "name": "boot",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:jamiesyme/comped-boot.git",
  "license": "MIT",
  "scripts": {
    "watch": "cd src && CHOKIDAR_USEPOLLING=1 parcel --no-hmr --no-cache --out-dir ../dist -p 3333 index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.1"
  }
}
